package com.example.studyspace;

import androidx.appcompat.app.AppCompatActivity;

public class QuestionManagementActivity extends AppCompatActivity {
    @Override
    protected void onCreate(android.os.Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // Kết nối với giao diện (layout).
        // Đảm bảo bạn đã có file layout tên là activity_question_management.xml
        // Nếu chưa có, hãy tạo nó trong res/layout hoặc đổi tên bên dưới cho đúng file bạn muốn dùng.
        setContentView(R.layout.tao_bo_de);
    }
}
