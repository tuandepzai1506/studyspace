<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studyspace">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Yêu cầu quyền ghi vào bộ nhớ cho các phiên bản Android cũ -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudySpace">
        <!-- Đăng ký Activity để thêm/sửa câu hỏi -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Study Space">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".QuestionManagementActivity"
            android:exported="false"
            android:label="@string/title_activity_question_management"
            android:theme="@style/Theme.StudySpace" />

        <activity
            android:name=".TaoBoDe"
            android:exported="false" />

        <activity
            android:name=".createQuizizz"
            android:exported="false" />
        <activity android:name=".Question_Bank"
            android:exported="false"/>
        <activity android:name=".createClass"
            android:exported="false"/>
        <activity android:name=".scoreTable"
            android:exported="false"/>
        <activity android:name=".classroom"
            android:exported="false"/>
        <activity
            android:name=".AddEditQuestionActivity"
            android:exported="false" />
        <activity
            android:name=".QuizPreviewActivity"
            android:exported="false"/>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>